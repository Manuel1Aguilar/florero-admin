<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login</title>
  <link rel="stylesheet" href="/styles.css">
  <script src="https://unpkg.com/htmx.org@1.6.1"></script>
</head>
<body>
  <div id="admin-content">
    <% if (!authenticated) { %>
      <h1>Admin Login</h1>
      <form action="/admin/login" method="POST" hx-post="/admin/login" hx-target="#admin-content">
        <div>
          <label for="admin_password">Password:</label>
          <input type="password" id="admin_password" name="admin_password" required>
        </div>
        <button type="submit">Login</button>
      </form>
    <% } else { %>
      <h2>Welcome, Admin!</h2>
      <p>Select an action:</p>
      <ul>
        <li><a href="/admin/manage-stock" hx-get="/admin/manage-stock" hx-target="#admin-content" hx-swap="innerHTML">Manage Stock</a></li>
        <li><a href="/admin/manage-orders" hx-get="/admin/manage-orders" hx-target="#admin-content" hx-swap="innerHTML">Manage Orders</a></li>
      </ul>
    <% } %>
  </div>
</body>
</html>
